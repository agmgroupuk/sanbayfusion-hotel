import type { Metadata } from "next";
import Image from "next/image";
import Link from "next/link";
import { CalendarDays, ChefHat, Music2, Users } from "lucide-react";
import { EventBookingForm } from "@/components/events/event-booking-form";
import { Reveal } from "@/components/motion/reveal";
import { site } from "@/lib/site";

export const metadata: Metadata = {
  title: "Events & Private Parties",
  description: "Plan a Sanbay Fusion private party, celebration, corporate gathering, or custom event for 50–100 guests.",
  alternates: { canonical: "/events" },
};

const eventCards = [["Private celebrations", "Birthdays, anniversaries, engagements, family gatherings and holiday moments.", CalendarDays], ["Food-led gatherings", "Thai, international, buffet, BBQ, finger food, desserts and premium hosted menus.", ChefHat], ["Atmosphere and production", "Music, DJ, sound, microphones and entertainment requirements shaped around your venue.", Music2]] as const;

export default function EventsPage() {
  return <div className="pb-28"><header className="mx-auto max-w-7xl px-5 pb-16 pt-36 sm:px-8 sm:pt-52"><Reveal variant="fade" className="text-eyebrow text-gold">Events, private parties & celebrations</Reveal><div className="mt-6 grid gap-10 lg:grid-cols-[1fr_0.8fr] lg:items-end"><div><h1 className="font-display text-h1 font-light">Make the occasion yours.</h1><p className="lead mt-7 max-w-2xl">A considered Sanbay Fusion event experience for home parties, private gatherings, corporate celebrations and custom occasions of approximately 50–100 guests.</p><div className="mt-8 flex flex-wrap gap-4"><a href="#event-builder" className="inline-flex rounded-full bg-gold px-7 py-3 text-eyebrow text-gold-foreground">Build Your Event</a><Link href="/contact" className="inline-flex rounded-full border border-foreground/30 px-7 py-3 text-eyebrow">Contact the Team</Link></div></div><Reveal variant="scale" delay={0.1} className="relative hidden aspect-[5/4] overflow-hidden rounded-sm lg:block"><Image src="/images/nicely-plated-food-served-at-decorated-table.jpg" alt="Sanbay Fusion food prepared for a private celebration" fill priority sizes="(min-width: 1024px) 38vw, 100vw" className="object-cover" /></Reveal></div></header><div className="mx-auto max-w-7xl px-5 sm:px-8"><section className="grid gap-5 md:grid-cols-3">{eventCards.map(([title, description, Icon], index) => <Reveal key={title} variant="up" delay={index * 0.05}><article className="h-full rounded-sm border border-border/60 bg-card/30 p-7"><Icon className="size-6 text-gold" /><h2 className="mt-7 font-display text-2xl font-light italic">{title}</h2><p className="mt-3 text-sm leading-relaxed text-foreground/70">{description}</p></article></Reveal>)}</section><section className="mt-20 grid gap-6 border-y border-border/60 py-10 md:grid-cols-3"><div><Users className="size-5 text-gold" /><p className="mt-4 text-eyebrow text-gold">Guest size</p><p className="mt-2 text-sm text-foreground/75">Designed around 50–100 guests, with exact counts welcome in the enquiry.</p></div><div><CalendarDays className="size-5 text-gold" /><p className="mt-4 text-eyebrow text-gold">Planned together</p><p className="mt-2 text-sm text-foreground/75">Date, duration, access, service level and menu direction are reviewed as one event brief.</p></div><div><ChefHat className="size-5 text-gold" /><p className="mt-4 text-eyebrow text-gold">Tailored proposal</p><p className="mt-2 text-sm text-foreground/75">Your selections guide a final proposal; this form does not create an automatic booking or quote.</p></div></section><section id="event-builder" className="mt-24 grid gap-12 lg:grid-cols-[minmax(0,1fr)_20rem] lg:items-start"><div><p className="text-eyebrow text-gold">Event builder</p><h2 className="mt-5 font-display text-4xl font-light sm:text-5xl">Shape the brief.</h2><p className="mt-5 max-w-2xl text-sm leading-relaxed text-muted-foreground">Select the services you are considering. The events team will confirm availability, quantities, staffing, legal alcohol requirements, venue logistics and final pricing with you.</p><div className="mt-10 rounded-sm border border-border/60 bg-card/30 p-6 sm:p-10"><EventBookingForm /></div></div><aside className="h-fit rounded-sm border border-gold/40 bg-gold/5 p-6 lg:sticky lg:top-28"><p className="text-eyebrow text-gold">Direct contact</p><h2 className="mt-5 font-display text-2xl font-light italic">Talk through the occasion.</h2><p className="mt-4 text-sm leading-relaxed text-foreground/75">For venue questions, complex production, larger guest counts or a fast conversation, contact the team directly.</p><address className="mt-6 space-y-2 text-sm not-italic text-foreground/80"><p><a className="hover:text-gold" href={`tel:${site.phone.replace(/\s/g, "")}`}>{site.phone}</a></p><p><a className="hover:text-gold" href={`mailto:${site.email}`}>{site.email}</a></p></address><Link href="/delivery-area" className="mt-7 inline-flex text-eyebrow text-gold underline decoration-gold/40 underline-offset-4">Check delivery area</Link></aside></section></div></div>;
}